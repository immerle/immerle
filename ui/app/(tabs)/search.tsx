/**
 * Placeholder for the "Recherche" tab route. Search itself is now a global
 * overlay (header popover on web, full-screen modal on mobile) — see
 * `SearchOverlay`. On mobile the tab press opens that overlay (intercepted in
 * the tabs layout) and never navigates here; on desktop the tab is hidden.
 * The file must exist so the tab route resolves.
 */
export default function SearchTabPlaceholder() {
  return null;
}
