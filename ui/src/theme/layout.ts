/**
 * Width (dp) at/above which the app switches from the mobile layout (bottom
 * tabs, compact player) to the desktop layout (left sidebar nav, full-width
 * docked player) — the Spotify-on-web shape.
 */
export const WIDE_BREAKPOINT = 800;
